# 2026-09-06

## Request

Update `README.md` and `CHANGELOG.md` by a comparison of the release branch
`release/2026-W37` against `main`. After the update, commit the work and update
the journal.

## Investigation

The branch holds 26 commits and 27 changed files, +2520/-72 lines.

To find the real content of the change, I read:

- `git log main..HEAD` and `git diff --name-status main..HEAD` for the file list.
- `git diff main..HEAD` on `.claude/CLAUDE.md`, `.tmux.conf`, `.vimrc`,
  `.config/atuin/config.toml`, `.config/btop/btop.conf`, `.config/kitty/kitty.conf`,
  `.config/lazygit/config.yml`, and `.config/nvim/lua/plugins/snacks.lua`.
- The new files `.claude/workflows/README.md`, `.claude/skills/ste_writing/SKILL.md`,
  `.config/espanso/match/dates.yml`, and `.config/nvim/scripts/tuxedo-dashboard.sh`.
- `journal/summary.md` and the existing `CHANGELOG.md` for the format and the last version.
- `git tag --sort=-v:refname` for the last tag, `v3.0.1`.

I also checked `.config/nvim/lazy-lock.json`. The diff holds commit-hash bumps only.
No plugin is added or removed. The changelog says this and does not list plugin names.

The last tag is `v3.0.1`. The branch adds features and breaks nothing, so the
new version is `3.1.0`.

## Changes

### CHANGELOG.md

Added the section `## [3.1.0] - 2026-09-06` above `## [3.0.1]`.

Added:

- Claude skills `graphify` and `ste_writing`.
- Claude workflow `debug-ticket` and `.claude/workflows/README.md`.
- New CLAUDE.md sections: Unix Philosophy, commit and journal conventions,
  secrets and sensitive files, and the rule to read official docs before use of a
  third-party API or config syntax.
- Espanso offset-date matches.
- The Neovim dashboard script `.config/nvim/scripts/tuxedo-dashboard.sh`.
- Tokyo Night theme files for atuin, bat, and kitty.
- Journal entries for 2026-08-12, 2026-08-13, and 2026-08-14.

Changed:

- The Neovim dashboard Tasks pane replaces the Projects and Git Status panes.
- Tokyo Night applied to atuin, bat, btop, kitty, code-server, VS Code, and vim.
- Tmux `allow-passthrough on`, `COLORTERM`, and `TERM_PROGRAM` for image rendering.
- The lazygit `pagers` key becomes `diffRenderers`.
- Neovim plugin versions in `lazy-lock.json`.
- New AWS and git permissions in `.claude/settings.local.json`.

### README.md

- Added `skills/` and `workflows/` under `.claude/` in the directory overview.
- Added `scripts/` to the Neovim tree.
- Added the section `### Espanso - Date Snippets` before `## Favorite Commands`.

## Corrections

The first draft of the espanso text said that each match asks for an offset in a
form. I then read `.config/espanso/match/dates.yml` and found regex triggers such
as `-18d` and `+2w`. I corrected the text in both files.

## Sanity check

Not run. The change is documentation only. This repository has no test suite for
Markdown.
